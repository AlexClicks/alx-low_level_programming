#include "main.h"
/**
 * _isdigit - main function
 *
 * @c: Character to compare
 *
 * Return: Either return 0 or 
 *
 */
int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
	{

		return (1);
	}
	return (0);
}
