#include "main.h"
/**
 * _abs - main function
 *
 * @n: interger to get the absolute
 *
 * Return: returns int or int times -ve
 *
 */
int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else
	{
		return (n * -1);
	}
}
